<!--pages/recommend/recommend.wxml-->
<view>

  <movie-review id="{{soudUrl}}" button="{{button}}" movie_image="{{movie_image}}" movie_id="{{movie_id}}" movie_name="{{movie_name}}"
  user="{{user}}"  soudUrl="{{soudUrl}}" review="{{input}}" recordingTime="{{recordingTime}}" bindFirstHandle="collectionHandle" bindSecondHandle="releaseHandle" bindAudioPlay="audioPlay"> </movie-review>
</view>